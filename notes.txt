NOTES:
DONE -- If user's first name is available, include it in welcome message (n8n).
DONE -- Email and password are not necessary BUT phone number is.
-- After a user pays, reset daily_message counter.
-- Add login with google or facebook BUT require phone number anyway (SMS verification).
-- Blocked message (n8n) should redirect to sign up page.


PLAN:
--- PHASE 1:
- Create auth, user sign in, sign up, and different roles (admin, user, affiliate).
- Create pay wall (pricing).
- Connect Stripe.
- Create secure pages (affiliate and admin).


--- PHASE 2:
- Create admin dashboard.
-- Stats (from: OpenAI, perplexity db, stripe, whish)
-- See all users table and be able to do CRUD operations.
-- See all affiliates and be able to do CRUD as well as change comission rules (%)
-- See contact messages. 
-- Settings change password only.

- Create referral system (if user entered through code, add code in user is_referred column).
- Create affiliate dashboard.
-- Code gen don't allow changes.
-- Referrals page.
-- Withdraw page with minimum threshhold feature.
-- Settings operations